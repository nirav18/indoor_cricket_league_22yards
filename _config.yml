# Enable GitHub Pages and jekyll-remote-theme
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme

# Custom CSS
sass:
  style: compressed

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: default

include:
  - assets/css/custom.css
